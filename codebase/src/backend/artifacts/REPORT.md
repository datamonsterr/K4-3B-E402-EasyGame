# Artifact report

Active artifacts define EasyGame behavior. `versions/v0/` is the initial immutable snapshot. `reference/starter_v0/` contains unmodified original Python tools and artifacts plus SHA-256 provenance; these files are excluded from TypeScript compilation and never loaded by the app. Run `npm run verify:structure` to verify integrity.

Evaluation covers deterministic notice selection, citation validation, import provenance and SLA boundaries. No LLM evaluation or live Discord result is claimed.
